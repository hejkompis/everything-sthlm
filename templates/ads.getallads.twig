<div class="container">
	<div class="row">
		<div class="col-xs-12">
			{% for ad in ads %}
				<div class="adListBox">
					<p><a href="?id={{ad.id}}">{{ad.title}}</a></p>
					<p>{{ad.dateCreated}}</p>
				</div>
			{% endfor %}	
		</div>
	</div>
</div>