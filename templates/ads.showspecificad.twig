<div class="container">
	<div class="row">
		<div class="col-xs-12">
			<div class="adBox">
				<h2>{{ad.title}}</h2>
					{% if ad.tags %}
						{% for tag in tags %}
							{% if tag.id in ad.tags %}<h4>{{tag.name}}{% endif %}</h4>
						{% endfor %}
					{% endif %}
				<p>{{ad.content}}</p>
				<h4>Ersättning/Bytes mot:</h4>
				<p>{{ad.payment}}</p>
				<p>Annonsen publicerades 
					{% if ad.createdDaysAgo == 0 %}
						idag.
					{% elseif ad.createdDaysAgo == 1 %}
						för {{ad.createdDaysAgo}} dag sedan.
					{% else %}
						för {{ad.createdDaysAgo}} dagar sedan.
					{% endif %}
				</p>

				<p>Typ av annons {{ad.typeName}}</p>
			</div>
		</div>
	</div>

	<div class="row">
		<div class="col-xs-12">
			{% if user and countInterest > 1 %}
				Det är {{countInterest}} andra personer som har visat intresse för denna annons. 
			{% elseif user and countInterest == 1 %}
				Det är en annan person som har visat intresse för denna annons.
			{% endif %}

			{% if user and not userInterest %}
				<a href="/ads/checkinterest/?id={{ad.id}}">Visa intresse</a>
			{% elseif user and userInterest %}	
				<a href="/ads/checkinterest/?id={{ad.id}}">Ångra intresse</a>
			{% else %}
				<a href="/user">Du måste logga in!</a>

			{% endif %}
		</div>
	</div>
	<br>
	<div class="row">
		<div class="col-xs-12">

			{% if user and ad.interested_users %} 
				Användare som har visat intresser för din annons:<br>
				{% for user in ad.interested_users %}
					{{user.firstname}} {{user.lastname}} {{user.email}}<br>
				{% endfor %}	
			{% endif %}
		</div>
	</div>

</div>











